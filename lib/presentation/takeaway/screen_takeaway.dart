import 'package:flutter/material.dart';

class ScreenTakeaway extends StatelessWidget {
  const ScreenTakeaway({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text('Takeaway'),
      ),
    );
  }
}
